#这个eureka的client其实就是服务的提供者
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    registerWithEureka: true
    fetchRegistry: true
server:
  port: 8762
spring:
  application:
    name: eureka-provide