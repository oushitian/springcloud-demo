1.父pom中一定要<spring-cloud.version>Finchley.SR1</spring-cloud.version>版本信息，不然导入不了eurake
2.注意，eureka启动后,访问http://localhost:8761就可以，不需要加/eureka
3.注意，eureka客户端也应该引入eureka-server的pom依赖
4.一个eureka的客户端要启动两个断口，可以修改edit configration中的配置，把single instance only的钩子去掉即可
5.增加新的boot模块时，简单做法是先创建一个远程仓库，把空目录clone下来，然后合并到本地工程目录，直接使用eclipse git插件上传就方便多了。